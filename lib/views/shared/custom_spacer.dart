import 'package:flutter/material.dart';

class CustomeSpacer extends StatelessWidget {
  const CustomeSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const  SizedBox(
      height:  25,

    );
  }
}